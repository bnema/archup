# archup barebone preset (~15 packages)
# Minimal CLI-only system for advanced users

# Core System
base
linux-firmware
btrfs-progs

# Kernel (selected dynamically by kernel.sh)
# linux / linux-lts / linux-zen

# Microcode (selected dynamically based on CPU detection)
# intel-ucode / amd-ucode

# Bootloader
limine
efibootmgr

# Networking
iwd

# Essential Tools
sudo
neovim
man-db
git

# Encryption (added if LUKS enabled)
# cryptsetup
